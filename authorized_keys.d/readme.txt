Drop ssh pub-key files into this directory, and they will be
concatenated into the '/root/.ssh/authorized_keys' file of the
generated debian live ISO.

Files with names that do not end with '.pub' are ignored.
